#!/bin/sh

echo "⚡ Running pre-push automated test suite..."

php artisan test
if [ $? -ne 0 ]; then
    echo "❌ Automated tests failed! Fix test errors before pushing."
    exit 1
fi

echo "✅ All tests passed! Pushing to remote repository..."
exit 0
